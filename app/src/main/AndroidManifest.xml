<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.unimag.miprimercultivo">

    <application
        android:allowBackup="true"
        android:icon="@drawable/pic1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/pic"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity
            android:name=".Example_Arenoso"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>

        <activity android:name=".Potato">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Example_Arenoso" />
        </activity>

        <activity android:name=".Example_Limoso">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Example_Limoso" />

        </activity>
        <activity android:name=".Example_Arcilloso">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Example_Arcilloso" />
        </activity>

        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Home"></activity>
    </application>

</manifest>